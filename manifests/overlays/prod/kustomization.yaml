apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

patches:
  - path: env-patch.yaml
  - path: replica-patch.yaml

images:
  - name: matteotv/argocd-demo
    newTag: prod-20260724-4
