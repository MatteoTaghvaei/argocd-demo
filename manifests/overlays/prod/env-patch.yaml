apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-argocd
spec:
  template:
    spec:
      containers:
        - name: demo-argocd
          env:
            - name: ENVIRONMENT
              value: "PROD"
            - name: VERSION
              value: "prod-unset"
            - name: BG_COLOR
              value: "#16a34a"
